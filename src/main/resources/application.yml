spring:
  application:
    name: example-service

  profiles:
    active: dev

#  flyway:
#    enabled: true
#    locations: classpath:/db/migration
#    out-of-order: true

server:
  port: 8080

management:
  server:
    port: 8081
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      probes:
        enabled: true